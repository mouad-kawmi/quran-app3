# Adhan Audio Files

The adhan notification sounds bundled in `android/app/src/main/res/raw/` are copied from the local user-provided `adhan/` folder.

The original filenames were normalized to Android raw-resource names so they can be used as scheduled notification sounds.
