name: native_adhan_bridge
description: Bridge for Native Adhan Scheduler.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

flutter:
  plugin:
    platforms:
      android:
        package: com.mouad.quran.native_adhan_bridge
        pluginClass: NativeAdhanBridgePlugin
